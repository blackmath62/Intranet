{% extends 'base.html.twig' %}

{% block title %}
	{{title}}
{% endblock %}

{% block body %}
	<div class="col-md-12">
		<!-- general form elements disabled -->
		<div class="card card-dark">
			<div class="card-header d-flex">
				<h3 class="card-title text-white">Modification de Société</h3>
				<a href="{{ path('app_admin_societe') }}" class="badge badge-success ml-auto">Retourner à la liste des Sociétés</a>
			</div>
			<div class="card-body">
				{{ form(societeEditForm)}}
				</div>
			</div>
		</div>
	</div>
{% endblock %}