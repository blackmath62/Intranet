{% extends 'base.html.twig' %}
{% block title %}{{ title }}{% endblock %}
{% block body %}
<div class="card collapsed-card card-warning">
    <div class="card-header">
        <h3 class="card-title"><i class="icon fas fa-exclamation-triangle"></i> Avertissement</h3>
        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse"><i class="fas fa-minus"></i></button>
        </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
                

    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card card-dark">
            <div class="card-header d-flex justify-content-between">
                <h3 class="card-title text-center">Email d'envoi général</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
           {{ form_start(formSendGeneralWithMails)}}
                <div class="form-group d-flex flex-wrap">
                    {{ form_widget(formSendGeneralWithMails.email) }}
                    {{ form_widget(formSendGeneralWithMails.Ajouter)}}
                </div>
            {{ form_end(formSendGeneralWithMails)}}
            {% if listeMailsGeneral %}
                <table id="example2" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th class="text-center"><span>Email</span></th>
                            <th class="text-center"><span>Action</span></th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for listeMailGeneral in listeMailsGeneral %}
                            <tr>
                                <td class="text-center">
                                    <a class="text-center text-dark">{{ listeMailGeneral.email }}</a>
                                </td>
                                <td class="text-center">
                                    <a data-toggle="modal" data-target="#modal-default-{{listeMailGeneral.id}}" class="text-center btn btn-xl btn-danger"><i class="fas fa-trash-alt"></i></a>
                                    <div class="modal fade" id="modal-default-{{listeMailGeneral.id}}">
                                        <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                            <h4 class="modal-title">Voulez vous vraiment supprimer ce mail ?</h4>
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                            </div>
                                            <div class="modal-body">
                                            <p>Cette action est irréversible !&hellip;</p>
                                            </div>
                                            <div class="modal-footer justify-content-between">
                                            <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                                            <a type="button" href='{{path('app_admin_email_delete', {'id' : listeMailGeneral.id } ) }}' class="btn btn-danger">Supprimer</a>
                                            </div>
                                        </div>
                                        <!-- /.modal-content -->
                                        </div>
                                        <!-- /.modal-dialog -->
                                    </div>
                                    <!-- /.modal -->
                                </td>
                            </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card card-dark">
            <div class="card-header d-flex justify-content-between">
                <h3 class="card-title text-center">Email de traitement</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
           {{ form_start(formSendTreatementWithMails)}}
                <div class="form-group d-flex flex-wrap">
                    {{ form_widget(formSendTreatementWithMails.email) }}
                    {{ form_widget(formSendTreatementWithMails.Ajouter)}}
                </div>
            {{ form_end(formSendTreatementWithMails)}}
            {% if listeMailsTreatement %}
                <table id="example2" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th class="text-center"><span>Email</span></th>
                            <th class="text-center"><span>Action</span></th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for listeMailTreatement in listeMailsTreatement %}
                            <tr>
                                <td class="text-center">
                                    <a class="text-center text-dark">{{ listeMailTreatement.email }}</a>
                                </td>
                                <td class="text-center">
                                    <a data-toggle="modal" data-target="#modal-default-{{listeMailTreatement.id}}" class="text-center btn btn-xl btn-danger"><i class="fas fa-trash-alt"></i></a>
                                    <div class="modal fade" id="modal-default-{{listeMailTreatement.id}}">
                                        <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                            <h4 class="modal-title">Voulez vous vraiment supprimer ce mail ?</h4>
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                            </div>
                                            <div class="modal-body">
                                            <p>Cette action est irréversible !&hellip;</p>
                                            </div>
                                            <div class="modal-footer justify-content-between">
                                            <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                                            <a type="button" href='{{path('app_admin_email_delete', {'id' : listeMailTreatement.id } ) }}' class="btn btn-danger">Supprimer</a>
                                            </div>
                                        </div>
                                        <!-- /.modal-content -->
                                        </div>
                                        <!-- /.modal-dialog -->
                                    </div>
                                    <!-- /.modal -->
                                </td>
                            </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
